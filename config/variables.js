const {
  DB,
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_CONNECTIONS,
  PORT,
  ACCESS_TOKEN_SECRET,
  REFRESH_TOKEN_SECRET,
  JWT_EXPIRATION
} = process.env;

module.exports = {
  DB,
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_CONNECTIONS,
  PORT,
  ACCESS_TOKEN_SECRET,
  REFRESH_TOKEN_SECRET,
  JWT_EXPIRATION
}