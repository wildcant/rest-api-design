const {
  DB,
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_CONNECTIONS,
  PORT,
  ACCESS_TOKEN_SECRET,
  REFRESH_TOKEN_SECRET,
  JWT_EXPIRATION,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  CORP_NAME,
} = process.env;

module.exports = {
  DB,
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_CONNECTIONS,
  PORT,
  ACCESS_TOKEN_SECRET,
  REFRESH_TOKEN_SECRET,
  JWT_EXPIRATION,
  MAIL_HOST,
  MAIL_PORT,
  MAIL_USER,
  MAIL_PASSWORD,
  CORP_NAME,
}